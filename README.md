# feedme

A pet project for food delivery services (UberEATS, Caviar, Postmates). Trying
to figure out which service gets me the cheapest delivery, and to provide nicer
UI features (e.g. sort by price, sort by delivery fee, sort by delivery time ->
then sort by price).